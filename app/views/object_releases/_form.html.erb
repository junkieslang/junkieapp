<%= form_for(@object_release) do |f| %>
  <% if @object_release.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@object_release.errors.count, "error") %> prohibited this object_release from being saved:</h2>

      <ul>
      <% @object_release.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
